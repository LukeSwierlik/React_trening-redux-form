const URL_API = 'https://rickandmortyapi.com/api/';
const API = {
    CHARACTERS: `${URL_API}character`,
    LOCATIONS: `${URL_API}location`,
    EPISODES: `${URL_API}episode`
};

export default API;
